spring:
  application.name: bench-marking
  cloud:
    consul:
      config:
        enabled: true
      enabled: true
      discovery:
        enabled: true

---
spring:
  profiles: local

  cloud:
    consul:
      config:
        enabled: false
      enabled: false
      discovery:
        enabled: false

