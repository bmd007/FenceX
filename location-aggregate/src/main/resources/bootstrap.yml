spring:
  application.name: location-aggregate
  cloud:
    consul:
      config:
        enabled: true
      enabled: true
      discovery:
        enabled: true
    loadbalancer:
      ribbon:
        enabled: false

---
spring:
  profiles: no-consul
  cloud:
    consul:
      config:
        enabled: false
      enabled: false
      discovery:
        enabled: false
