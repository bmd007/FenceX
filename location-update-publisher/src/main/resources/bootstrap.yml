spring:
  application.name: location-update-publisher
  cloud:
    consul:
      config:
        enabled: true
      enabled: true
      discovery:
        enabled: true
    loadbalancer:
      ribbon:
        enabled: false

---
spring:
  profiles: local
  cloud:
    consul:
      config:
        enabled: false
      enabled: false
      discovery:
        enabled: false
