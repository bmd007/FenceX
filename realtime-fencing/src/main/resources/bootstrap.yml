spring:
  application.name: realtime-fencing
  cloud:
    consul:
      config:
        enabled: true
      enabled: true
      discovery:
        enabled: true
    loadbalancer:
      ribbon:
        enabled: false

---
spring:
  profiles: local
  cloud:
    consul:
      config:
        enabled: false
      enabled: false
      discovery:
        enabled: false
